package ar.edu.utn.frba.dds.server.routers;

import io.javalin.config.JavalinConfig;

public class IncidenteRouter implements IRouter {

    @Override
    public void apply(JavalinConfig config) {
        // TODO - endpoint incidentes
    }
}
